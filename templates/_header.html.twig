{#  Start Home Section  #}
<div id="home">

	{# Start Nav  #}
	<nav class="navbar navbar-expand-lg fixed-top solid">
        <div class="container">
			<div class="crop-logo">
				<img src="{{ asset('images/brand/logo.png') }}" alt=""/>
				<a class="navbar-brand" href="{{ path('homepage') }}">EspoirSurPattes</a>
			</div>
            
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive">
                <span class="custom-toggler-icon"><i class="fas fa-bars"></i></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarResponsive">
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item ml-auto">
                        <a href="{{ path('map') }}" class="nav-link">Carte</a>
                    </li>
                    <li class="nav-item ml-auto">
                        <a href="{{ path('adoption') }}" class="nav-link">Adoption</a>
                    </li>
                    <li class="nav-item ml-auto">
                        <a href="{{ path('blog') }}" class="nav-link">News</a>
                    </li>
                    <li class="nav-item ml-auto">
                        <a href="{{ path('contact') }}" class="nav-link">Contact</a>
                    </li>
					<li class="nav-item ml-auto">
                        <a href="{{ path('association') }}" class="nav-link">Associations</a>
                    </li>
                    <li class="nav-item ml-auto">
                        <a href="{{ path('about') }}" class="nav-link">A propos</a>
                    </li>
					{% if app.user %}
					<li class="nav-item ml-auto">
                        <a href="{{ path('app_logout') }}" class="nav-link">Déconnexion</a>
                    </li>
					{% else %}
                    <li class="nav-item ml-auto">
                        <a href="{{ path('app_login') }}" class="nav-link">Connexion</a>
                    </li>
                    
                    <li class="nav-item ml-auto">
                        <a href="{{ path('create_account') }}" class="nav-link">Inscription</a>
                    </li>
					{% endif %}
                </ul>
            </div>
        </div>
    </nav>
	{# End Nav #}
</div>
{# End Home Section #}