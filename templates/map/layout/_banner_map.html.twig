{# Start Banner  #}
<div class="banner-headline-card d-flex flex-column justify-content-center">
    <div class="container">
        <div class="card card-banner">
            <div class="card-body card-banner text-center d-lg-flex flex-lg-column align-items-lg-end justify-content-lg-center">
                <h5 class="card-title">Vous avez perdu ou trouvé un animal ?</h5>
                {% if app.user %}
                <a href="{{ path ('animal_create') }}" class="btn btn-primary">Faire un signalement</a>
                {% endif %}
                {% if app.user == false %}
                <a href="{{ path ('create_account') }}" class="btn btn-primary">S'inscrire pour faire un signalement</a>
                {% endif %}
            </div>
        </div>
    </div>
</div>
{# End Banner #}
